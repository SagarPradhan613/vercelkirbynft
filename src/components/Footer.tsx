import React, { ReactElement } from "react";
import {
    Col,
} from "react-bootstrap";

const Footer = ():ReactElement<React.FC>=>{
    return(
        <Col className="footer mt-3">
            
        </Col>
    )
}
export default Footer;